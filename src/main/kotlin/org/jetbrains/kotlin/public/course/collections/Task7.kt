package org.jetbrains.kotlin.public.course.collections

fun main() {
//    println(twoSum(listOf(2, 7, 11, 15), 9)) // Pair(0, 1) because 2 + 7 = 9
//    println(twoSum(listOf(3, 2, 4), 6)) // Pair(1, 2) because 2 + 4 = 6
//    println(twoSum(listOf(3, 3), 6)) // Pair(0, 1) because 3 + 3 = 6
//    println(twoSum(listOf(1, 2, 3), 7)) // null
}

// Implement the function twoSum that finds two indices in the list
// whose corresponding values add up to the target.
// Return the indices as a Pair, or null if no such pair exists.
// The solution MUST work in O(n) time
