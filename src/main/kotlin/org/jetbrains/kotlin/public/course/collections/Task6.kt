package org.jetbrains.kotlin.public.course.collections

fun main() {
//    val merged1 = mergeSorted(listOf(1, 3, 5, 7), listOf(2, 4, 6, 8))
//    println(merged1) // [1, 2, 3, 4, 5, 6, 7, 8]
//
//    val merged2 = mergeSorted(listOf(1, 1, 3), listOf(2, 3, 4))
//    println(merged2) // [1, 1, 2, 3, 3, 4]
//
//    val merged3 = mergeSorted(emptyList(), listOf(1, 2, 3))
//    println(merged3) // [1, 2, 3]
//
//    val merged4 = mergeSorted(setOf(1, 3, 5), listOf(2, 4, 6))
//    println(merged4) // [1, 2, 3, 4, 5, 6]
}

// Implement the function mergeSorted that takes two sorted collections
// and merges them into a single sorted list.
// Both input collections are already sorted in ascending order.
// The result should also be sorted.
//
// Think carefully about the parameter types.
